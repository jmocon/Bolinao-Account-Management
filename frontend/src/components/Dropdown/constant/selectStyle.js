const selectStyle = {
  control: (baseStyles) => ({
    ...baseStyles,
    borderColor: 'rgba(29, 37, 59, 0.5)',
    borderRadius: '0.4285rem'
  }),
  input: (baseStyles) => ({
    ...baseStyles,
    marginTop: '0px',
    marginBottom: '0px',
    paddingTop: '0px',
    paddingBottom: '0px'
  })
};

export default selectStyle;
